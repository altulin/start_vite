import clsx from "clsx";
import { FC } from "react";
import style from "./Promo.module.scss";

const Promo: FC = () => {
  return <section className={clsx(style.promo)}>Promo</section>;
};
export default Promo;
