import clsx from "clsx";
import { FC } from "react";
import style from "./Slider.module.scss";

const Slider: FC = () => {
  return <section className={clsx(style.slider)}></section>;
};
export default Slider;
